sudo: required
dist: trusty
language: node_js
node_js:
  - "8.11.4"
branches:
  only:
  - master

before_script:
- npm install -g angular-cli
- npm install

script:
- ng test --watch=false --browsers=ChromeHeadless
 